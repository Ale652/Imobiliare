import { styled } from "@mui/material/styles";

export const MainContainer = styled("a")({})

export const CardSaleLink = styled("div")({
    position: "relative"
})

export const Image = styled("img")({
    height: "209px",
    width: "281px",
})

export const Type = styled("div")({
    position: "absolute",
    display: "flex",
    flexDirection: "column",
})

